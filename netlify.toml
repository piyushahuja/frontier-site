# index.html is generated by code/build.py and committed to the repo.
# Netlify serves it as-is and does NOT run the build — so run
# `python3 code/build.py` and commit index.html before deploying.
[build]
  publish = "."
